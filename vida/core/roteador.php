<?php

function rotear($method, $caminho) {
    if ($method == "GET") {
        if ($caminho == "usuarios") {
            // include_once __DIR__ . "/../api/usuarios/usuarios.php";
        }
    } else if ($method == "POST") {
        //
    }
}